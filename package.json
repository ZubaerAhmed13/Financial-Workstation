{
  "name": "financial-workstation-certification",
  "version": "1.1.0",
  "private": true,
  "description": "Deterministic financial calculation certification and single-file build tooling for Financial Workstation",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "build:root": "node scripts/build.js --write-root",
    "verify": "npm test && npm run build && node scripts/verify-build.js"
  },
  "engines": {"node": ">=20"}
}
